version: 2

jobs:
  "4.14.138":
    working_directory: ~/linux-kernel-ci
    docker:
      - image: pexcn/docker-images:stretch-linux-kernel
    environment:
      KERNEL_VERSION: 4.14.138
    steps:
      - checkout
      - run:
          name: Run build
          command: ./build.sh
      - run:
          name: Deploy
          command: ./deploy.sh
      - store_artifacts:
          path: release
          destination: packages
  "4.19.66":
    working_directory: ~/linux-kernel-ci
    docker:
      - image: pexcn/docker-images:stretch-linux-kernel
    environment:
      KERNEL_VERSION: 4.19.66
    steps:
      - checkout
      - run:
          name: Run build
          command: ./build.sh
      - run:
          name: Deploy
          command: ./deploy.sh
      - store_artifacts:
          path: release
          destination: packages
  "5.2.8":
    working_directory: ~/linux-kernel-ci
    docker:
      - image: pexcn/docker-images:stretch-linux-kernel
    environment:
      KERNEL_VERSION: 5.2.8
    steps:
      - checkout
      - run:
          name: Run build
          command: ./build.sh
      - run:
          name: Deploy
          command: ./deploy.sh
      - store_artifacts:
          path: release
          destination: packages

workflows:
  version: 2
  build:
    jobs:
      - "4.14.138"
      - "4.19.66"
      - "5.2.8"
